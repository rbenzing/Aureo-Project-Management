# =============================================================================
# Aureo Project Management — .gitignore
# =============================================================================
# Policy: commit only sources, the schema-of-record migration, and config
# required for reproducible installs. Everything generated, environment-
# specific, or transient stays out.
# =============================================================================


# -----------------------------------------------------------------------------
# Secrets, env, credentials  (NEVER commit)
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example

# docs/DEPLOYMENT.md tells operators to create these by hand on the server;
# they hold DB credentials and PASSWORD_PEPPER. On a clone-based install they
# land inside the work tree, where `git add -A` would otherwise stage them.
/config/config.php
/config/config-path.php

# Written by the installer (web or bin/setup.php) on success.
# installed.lock would ship in the release archive and permanently disable
# the installer for everyone who downloaded it. aureo-config.php is the
# *preferred* config target under the recommended layout - dirname(DOCUMENT_ROOT)
# is the repository root itself there, so a local install run produces an
# untracked credentials file sitting at the repo root, one `git add .` away
# from being published. In the drop-in layout the same candidate lands
# outside the repository and git never sees it either way.
/config/installed.lock
/aureo-config.php

*.key
*.pem
*.p12
*.pfx
*.ppk
*.crt
auth.json
secrets/
.secrets/
docs/superpowers/*
docs/specs/*

# -----------------------------------------------------------------------------
# PHP / Composer
# -----------------------------------------------------------------------------
/vendor/
composer.phar
.php-cs-fixer.cache
.phpunit.cache/
.phpunit.result.cache
.phpstan.cache/
.psalm-cache/
/coverage/
/coverage.xml
/clover.xml
/report.xml


# -----------------------------------------------------------------------------
# Database — Phinx migrations & seeds
# -----------------------------------------------------------------------------
# The canonical migration in db/migrations/ IS committed; `composer setup`
# depends on it. Ignore only generated scratch files, editor backups, and
# developer-local experiments that must not reach main.
db/migrations/*.bak
db/migrations/*.tmp
db/migrations/*.orig
db/migrations/*.swp
db/migrations/*.local.php
db/migrations/_*.php
db/migrations/scratch/
db/seeds/*.bak
db/seeds/*.tmp
db/seeds/*.local.php
db/seeds/scratch/

# Raw SQL dumps and one-off scripts produced during development
*.sql.bak
*.dump
*.dump.sql
dump.sql
dump_*.sql
backup_*.sql
*.mwb.bak


# -----------------------------------------------------------------------------
# Logs (the app writes to /log; webserver logs may also land in tree)
# -----------------------------------------------------------------------------
*.log
log/
logs/
storage/logs/
npm-debug.log*
yarn-error.log*


# -----------------------------------------------------------------------------
# Cache / runtime / temp
# -----------------------------------------------------------------------------
cache/
tmp/
temp/
var/cache/
var/tmp/
var/sessions/
storage/framework/cache/
storage/framework/sessions/
storage/framework/views/
sessions/
.session


# -----------------------------------------------------------------------------
# Node / Tailwind / build artifacts
# -----------------------------------------------------------------------------
/node_modules/
yarn.lock
pnpm-lock.yaml
.npm/
.yarn/
.pnp.*
.tailwindcss/
tailwind.config.cache
postcss.cache/

# Compiled CSS/JS — keep sources, not output
/public/assets/css/styles.css
/public/css/output.css
/public/js/*.bundle.js
/public/build/
/public/dist/


# -----------------------------------------------------------------------------
# Local tooling installed via composer scripts (phpMyAdmin etc.)
# -----------------------------------------------------------------------------
/tools/


# -----------------------------------------------------------------------------
# User uploads / runtime media (per-environment, never committed)
# -----------------------------------------------------------------------------
/uploads/
/public/uploads/
/storage/app/public/


# -----------------------------------------------------------------------------
# IDE / editor
# -----------------------------------------------------------------------------
.idea/
*.iml
.vscode/
*.code-workspace
.history/
*.sublime-project
*.sublime-workspace
.settings/
.project
.classpath
.buildpath
nbproject/
*.swp
*.swo
*~


# -----------------------------------------------------------------------------
# OS metadata
# -----------------------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/


# -----------------------------------------------------------------------------
# Generic temp/backup file patterns
# -----------------------------------------------------------------------------
*.bak
*.backup
*.tmp
*.temp
*.orig
*.rej
*.old


# -----------------------------------------------------------------------------
# Local scratch pages at the repo root
# -----------------------------------------------------------------------------
# Standalone marketing/landing pages kept locally. Not part of the application —
# nothing under src/ or public/ references them, and they are not routed.
/default.php


# -----------------------------------------------------------------------------
# Claude / agent local state
# -----------------------------------------------------------------------------
# .claude/CLAUDE.md IS committed (agentic project guidance). Per-developer
# memory, settings, and orchestrator output are not.
/.claude/memory/
/.claude/settings.local.json
/.claude/cache/
/.claude/artifacts/
/.claude/orchestrator/
