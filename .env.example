# Database Configuration
DB_HOST=localhost:3306
DB_NAME=your_database_name
DB_USERNAME=your_database_user
DB_PASSWORD=your_secure_database_password
DB_CHARSET=utf8mb4

# Email Configuration
SMTP_HOST=smtp.example.com
SMTP_USERNAME=your-email@example.com
SMTP_PASSWORD=your-password
SMTP_PORT=587
SMTP_DEBUG=0 # set to 0 for prod, 2 for debug
EMAIL_FROM=no-reply@example.com
EMAIL_FROM_NAME=Aureo

# Security Settings
CSRF_TOKEN_EXPIRY=3600
# RESERVED, CURRENTLY UNUSED. No code reads PASSWORD_PEPPER - passwords are
# hashed by App\Utils\PasswordHasher with no pepper applied. Setting it has no
# effect today; it is kept so an installation that later gains peppering does
# not need a config change.
PASSWORD_PEPPER=your-secret-pepper-string
SESSION_SECURE=true
SESSION_HTTP_ONLY=true

# Application Settings
APP_ENV=production
APP_DEBUG=false
APP_TIMEZONE=UTC
APP_DOMAIN=domain.com
APP_COMPANY="Company Name"
APP_SCHEME=https
APP_LOCALE=en_US
APP_CURRENCY_FORMAT=%i